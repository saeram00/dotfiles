# File manipulation
alias cp='cp -iv'
alias mv='mv -iv'
alias rm='rm -Iv'
alias chmod='chmod -c'
alias sln='ln -siv'

# File listing (with lsd)
#alias ls='lsd'
#alias l.='lsd -dF .*'
#alias ll='lsd -AhlF'
#alias la='lsd -AF'
#alias li='lsd -dlF'

# Go to Windows User Home Directory (WSL specific)
#alias cdw='cd /mnt/c/Users/matia'
#alias cdv='cd $VAULT_DIR'

# Clear screen and history
alias hsc='history -c'
alias clh='clear && history -c'
alias cls='clear && ls'
alias cll='clear && ls -AhlF'
alias cla='clear && ls -AF'

# Resource management
alias df='df -h'
alias free='free -h'
alias du='du -h'

# Network
alias wget='wget --show-progress'
alias scurl='curl --tlsv1.3 --proto "=https"'
alias scurl-dl='curl --tlsv1.3 --proto "=https" --location --remote-name'

# Programs
alias vim='vim.nox'
alias nv='nvim'
alias nvdiff='nvim -d'

# Authentication
alias git-ssh='ssh -T saeram-github'
alias ssh-start-agent='eval $(ssh-agent -s)'
